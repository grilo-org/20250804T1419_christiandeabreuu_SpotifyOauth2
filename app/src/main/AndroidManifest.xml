<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_spotify_full"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_spotify_full"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity
            android:name=".app.ui.createplaylist.CreatePlaylistActivity"
            android:exported="false" />
        <activity
            android:name=".app.ui.playlist.PlaylistActivity"
            android:exported="false" />
        <activity
            android:name=".app.ui.profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".authenticate.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="callback"
                    android:scheme="meuapp" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".app.ui.topartists.TopArtistsActivity"
            android:exported="false" />
    </application>

</manifest>